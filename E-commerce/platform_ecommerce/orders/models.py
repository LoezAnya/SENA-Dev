"""
Sprint 1 scope: this app exists as scaffolding only (registered in
INSTALLED_APPS so migrations/tests are wired up), so that Sprint 3
(payment gateway, Order model, cart) can be added without restructuring
the project. No models are defined yet.
"""
